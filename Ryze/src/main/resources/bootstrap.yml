spring:
  application:
    name: ryze
  cloud:
    nacos:
      discovery:
        server-addr: { nacos.address }

management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  file:
    name: ${logging.path}/${spring.application.name}_info.log