spring:
  application:
    name: yorick

  cloud:
    config:
      server:
        git:
          clone-on-start: true
          force-pull: true
          timeout: 60

    nacos:
      discovery:
        server-addr: ${nacos.address}

logging:
  file:
    name: ${logging.path}${spring.application.name}_info.log

encrypt:
  key: osris