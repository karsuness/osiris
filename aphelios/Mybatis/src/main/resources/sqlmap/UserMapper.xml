<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace：命名空间，用于隔离sql，还有一个很重要的作用，后面会讲 -->
<mapper namespace="com.wjx.mybatis02.dao.UserDao">
    <select id="getUserById" parameterType="java.lang.Integer" resultType="com.wjx.mybatis01.domain.User">
        select * from user where id = #{value};
    </select>

    <select id="getUserByName" parameterType="java.lang.String" resultType="com.wjx.mybatis01.domain.User">
        select * from user where username like "%"#{value}"%";
    </select>

    <select id="listUser" resultType="com.wjx.mybatis01.domain.User">
        select * from user;
    </select>


    <insert id="insertUser" parameterType="com.wjx.mybatis01.domain.User">
        insert into user(
        username,
        sex,
        birthday,
        address
        )values(
        #{username},
        #{sex},
        #{birthday},
        #{address}
        )
    </insert>

    <update id="updateUserById" parameterType="com.wjx.mybatis01.domain.User">
        update user
        set
        username = #{username},
        sex = #{sex},
        birthday = #{birthday},
        address = #{address}
		where id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from user where id = #{value};
    </delete>
</mapper>
